$(document).ready(function () {
    /* Data */
    $("section.summary-section").load('/includes/' + lang + 'summary-section.html');

    $("section.experience-section").load('/includes/' + lang + 'experience-section.html');
    $("section.skills-section").load('/includes/' + lang + 'skills-section.html');
    $("section.education-section").load('/includes/' + lang + 'education-section.html');

    $("section.reference-section").load('/includes/' + lang + 'reference-section.html');
    $("section.language-section").load('/includes/' + lang + 'language-section.html');
    $("section.interests-section").load('/includes/' + lang + 'interests-section.html');


});
